rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write operations (local auth, no Firebase Auth)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
